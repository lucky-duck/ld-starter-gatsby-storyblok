import React from "react";
import Welcome from "../../../temporary/welcome";

function storyblokWelcome({ text, title }) {
  return <Welcome text={text} title={title} />;
}

export default storyblokWelcome;
